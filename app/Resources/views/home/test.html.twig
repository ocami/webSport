{% extends 'base.html.twig' %}

{% block title %}
    {{ parent() }} index
{% endblock %}

{% block body %}

    <h1>Test</h1>

    {% for l in test %}
            <h3>{{ l[0].name }}</h3>
            {% for rc in l[1] %}
                {{ rc.competitor.firstName }} {{ rc.competitor.lastName }} {{ rc.chrono|date('H:i:s') }}<br>
            {% endfor %}
    {% endfor %}

{% endblock %}